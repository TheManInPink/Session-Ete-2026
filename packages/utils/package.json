{
  "name": "@nina-aes/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Utilitaires partagés (NINA, Merkle, crypto, sanitize) pour la plateforme NINA-AES.",
  "keywords": ["nina", "aes", "platform", "utils", "merkle", "jwt"],
  "author": "Étudiant UQAR",
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist .turbo",
    "lint": "eslint \"src/**/*.ts\"",
    "check-types": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.5.2",
    "jest": "^30.3.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.4.9",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=24.14.0",
    "pnpm": ">=10.0.0"
  }
}
