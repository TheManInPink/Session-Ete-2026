# ── apps/admin — variables d'environnement ────────────────────────────────────
# Copier ce fichier vers `.env.local` et adapter les valeurs.

# URL de base de l'API NINA-AES (côté serveur RSC — réseau interne K8s en prod)
API_BASE_URL=http://localhost:3000

# Issuer Keycloak (utilisé pour OIDC PKCE — doc 08)
KEYCLOAK_ISSUER=http://localhost:8080/realms/nina-aes
KEYCLOAK_CLIENT_ID=nina-admin

# URL publique de l'app (utilisée pour redirect_uri OIDC)
APP_PUBLIC_URL=http://localhost:4002

# Mode d'authentification — `mock` retourne une session déterministe « agent
# CTDEC fictif » sans dépendance Keycloak. Bascule sur `keycloak` une fois le
# realm-config et l'auth-service prêts.
NINA_AUTH_MODE=mock

# Secrets server-only — JAMAIS exposés au client
KEYCLOAK_CLIENT_SECRET=
SESSION_COOKIE_SECRET=
