# NINA-AES — Pipeline d'entraînement des modèles IA
# Installer avec : pip install -r requirements.txt
# (ou, recommandé : pip install -e .  — cf. README.md)
#
# Versions alignées sur l'environnement vérifié (Python 3.14).

scikit-learn>=1.8.0
xgboost>=3.2.0
pandas>=2.3.3
numpy>=2.3.5
scipy>=1.16.3
joblib>=1.5.2
rapidfuzz>=3.14.3
jellyfish>=1.1.0

# Optionnel — tracking d'expériences (repli JSON si absent).
mlflow>=2.20.0

# Outillage dev (tests + lint).
pytest>=9.0.3
ruff>=0.11.0
